
# this file is "conf.d/http-inner.conf", loaded in as an include under main http

allow localhost;
deny 192.168.1.0/24;

# let's delete this as a test
expires max;

# end of conf.d/http-inner.conf (but still under http block)
