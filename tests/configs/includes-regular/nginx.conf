# empty events for now
events {
     multi_accept on;
}

# and all http via included configs
# m'kay?
http {

    map $what $kind {
      banana fruit;
      beans vegetable;
      brains animal;
    }

    include conf.d/server.conf;
    include conf.d/http*.conf;
}
