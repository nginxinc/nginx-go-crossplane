#@ production
server {
    #listen      127.0.0.1:8080;
    listen      :8080;
    server_name foo;
    # including details.conf into server block
    include     details.conf;
    # include     bar.conf;

    #@ ramones
    location /heynow {
        return 200 "Gaba Gaba Hey!";
    }

    # not tagged
    location /nada {
	#@ hellaworld
        location /nada/problem {
            return 202 "what me worry?";
	}
        return 201 "say what?";
    }

    # not tagged either
    location /meh {
        return 203 "what ev er";
    }
}
