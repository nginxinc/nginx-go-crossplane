
user nginx;
worker_processes auto;

events {}

http {
    server {
        listen       80 default_server;
        listen       [::]:80 default_server;
        server_name  _;
        root         /usr/share/nginx/html;

        location /foo {
        }

        location / {
	    return 200 "nothing here";
        }

    }
}
